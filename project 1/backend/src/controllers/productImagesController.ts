import { Request, Response } from 'express';
import pool from '../database/connection';

export class ProductImagesController{
  static addProductImage = async(req: Request, res: Response): Promise<void> => {
    try{

    }catch(error){
      
    }
  }
}