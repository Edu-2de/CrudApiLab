import { Request, Response } from 'express';
import pool from '../database/connection';

export class OrderController {
  static addOrder = async (req: Request, res: Response): Promise<void> => {
    try {
      
    } catch (error) {

    }
  };
}
